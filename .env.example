# =============================================================================
# OKRs Tracker - Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values
# Never commit .env.local to version control

# =============================================================================
# SUPABASE (Required)
# Get these values from your Supabase project: Settings → API
# =============================================================================

# Project URL (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Publishable/Anon Key (required)
# This is safe to expose in the browser - used for client-side auth
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-anon-key

# Service Role Key (optional, for server-side admin operations)
# WARNING: Never expose this in the browser - has full database access
SUPABASE_SECRET_KEY=your-service-role-secret-key

# =============================================================================
# APPLICATION (Optional)
# =============================================================================

# Site URL for OAuth redirects (defaults to http://localhost:3000 in development)
# Set this in production to your actual domain
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# =============================================================================
# DEVELOPMENT/BUILD (Optional)
# =============================================================================

# Enable bundle analyzer: npm run build with ANALYZE=true
# ANALYZE=true
